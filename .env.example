OPENROUTER_API_KEY=        # LLM calls via OpenRouter (required)
SARVAM_API_KEY=            # STT and TTS for Indic languages
OPENAI_API_KEY=            # Embeddings, Whisper STT, TTS English, GPT-4o vision
COHERE_API_KEY=            # Rerank cross-encoder
QDRANT_URL=                # Vector store URL (leave blank to use in-memory)
QDRANT_API_KEY=            # Vector store auth (not needed for in-memory)
USE_CLOUD_QDRANT=false     # true = use Cloud Qdrant (if URL present), false = force in-memory
USE_SUPABASE=true          # true = use Supabase for durable jobs/doc metadata, false = in-memory fallback
SUPABASE_URL=              # Supabase project URL (https://<project-ref>.supabase.co)
SUPABASE_SERVICE_ROLE_KEY= # Supabase service_role key (server-side only)
SUPABASE_JOBS_TABLE=ingest_jobs   # optional override
SUPABASE_DOCS_TABLE=documents     # optional override
LLAMAPARSE_API_KEY=        # Table-aware PDF parsing
USE_MOCKS=false            # Set to true to mock all external APIs
RAG_VEC_K=20              # Vector candidate pool size
RAG_BM25_K=20             # BM25 candidate pool size
RAG_RERANK_K=8            # How many candidates to rerank
RAG_TOP_N=3               # Final chunks sent to generator
RAG_TOP_K=7               # Final retrieved results kept pre-rerank
RAG_CONF_THRESHOLD=0.35   # Confidence gate threshold for refusal
STT_CONFIDENCE_THRESHOLD=0.7 # Voice retry threshold
SSL_CERT_FILE=             # Path to CA bundle (certs/ca-bundle.pem for corporate networks)
REQUESTS_CA_BUNDLE=        # Same as SSL_CERT_FILE for requests-based libs
